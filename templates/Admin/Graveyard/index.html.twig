{% extends 'Admin/Common/Dashboard/layout.html.twig' %}
{% block title %}{{ app_name }} - {{ 'ui.admin.graveyard.title.index'|trans }}{% endblock %}
{% block dashboard_content %}
    <!-- Dashboard Card -->
    {% embed 'Admin/Common/Dashboard/_dashboardCard.html.twig' %}
        {% block title %}
            <!-- Dashboard Title -->
            <h1 class="text-slate-900 dark:text-slate-100">{{ 'ui.admin.graveyard.title.index'|trans }}</h1>
            <h2 class="text-gray-500 dark:text-gray-300">{{ 'ui.admin.graveyard.description.index.manage'|trans }}</h2>
            <!-- /Dashboard Title -->
        {% endblock %}
        {% block dashboard_content %}
            <!-- Dashboard Content -->
            <!-- Pagination -->
            {% embed 'Core/Pagination/pagination.html.twig' %}
                {% block head %}
                    {% include 'Core/Pagination/_head.html.twig' with {
                        label: 'ui.admin.graveyard.name',
                        name: 'g.name',
                    } %}
                    {% include 'Core/Pagination/_head.html.twig' with {
                        label: 'ui.common.updatedAt',
                        name: 'g.updatedAt',
                        class: 'text-center hidden md:block'
                    } %}
                    {% include 'Core/Pagination/_head.html.twig' with {
                        label: 'ui.pagination.edit'|trans,
                    } %}
                {% endblock %}
                {% block body %}
                    {% include 'Core/Pagination/_body_value.html.twig' with {
                        value: item.name,
                    } %}
                    {% include 'Core/Pagination/_body_value.html.twig' with {
                        value: item.updatedAt|date('Y-m-d'),
                        class: 'text-center hidden md:block'
                    } %}
                    {% embed 'Core/Pagination/_body_action.html.twig' %}
                        {% block actions %}
                        {% endblock %}
                    {% endembed %}
                {% endblock%}
            {% endembed %}
            <!-- /Pagination -->
            <!-- /Dashboard Content -->
        {% endblock %}
        {% block buttons_after %}
        {% endblock %}
    {% endembed %}
    <!-- /Dashboard Card -->
{% endblock %}


